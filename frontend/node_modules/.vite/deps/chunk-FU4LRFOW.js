import {
  getOverflowParents
} from "./chunk-VAVOZAH6.js";
import {
  createSingletonState,
  findUpUntil
} from "./chunk-G6NP7Q4A.js";
import {
  __toESM,
  require_react
} from "./chunk-2YIMICFJ.js";

// node_modules/@cloudscape-design/components/internal/utils/calculate-once.js
function calculateOnce(callback) {
  let result = void 0;
  return () => {
    if (result === void 0) {
      result = callback();
    }
    return result;
  };
}

// node_modules/@cloudscape-design/components/internal/utils/browser-scrollbar-size.js
var browserScrollbarSize = calculateOnce(() => {
  if (typeof document === "undefined") {
    return { width: 0, height: 0 };
  }
  const scrollDiv = document.createElement("div");
  scrollDiv.style.overflow = "scroll";
  scrollDiv.style.height = "100px";
  scrollDiv.style.width = "100px";
  scrollDiv.style.position = "absolute";
  scrollDiv.style.top = "-9999px";
  scrollDiv.style.left = "-9999px";
  document.body.appendChild(scrollDiv);
  const width = scrollDiv.offsetWidth - scrollDiv.clientWidth;
  const height = scrollDiv.offsetHeight - scrollDiv.clientHeight;
  document.body.removeChild(scrollDiv);
  return { width, height };
});

// node_modules/@cloudscape-design/components/internal/breakpoints.js
var BREAKPOINT_MAPPING = [
  ["xl", 1840],
  ["l", 1320],
  ["m", 1120],
  ["s", 912],
  ["xs", 688],
  ["xxs", 465],
  ["default", -1]
];
var mobileBreakpoint = BREAKPOINT_MAPPING.filter((b) => b[0] === "xs")[0][1];
var BREAKPOINTS_DESCENDING = BREAKPOINT_MAPPING.map(([bp]) => bp);
function matchBreakpointMapping(subset, actual) {
  const qualifyingBreakpoints = BREAKPOINT_MAPPING.slice(BREAKPOINTS_DESCENDING.indexOf(actual));
  for (const [breakpoint] of qualifyingBreakpoints) {
    const breakpointValue = subset[breakpoint];
    if (breakpointValue !== void 0) {
      return breakpointValue;
    }
  }
  return null;
}
function getMatchingBreakpoint(width, breakpointFilter, previousBreakpoint) {
  const breakpointSwitchOffset = browserScrollbarSize().width;
  const previousBreakpointIndex = previousBreakpoint === void 0 || previousBreakpoint === null ? -1 : BREAKPOINTS_DESCENDING.indexOf(previousBreakpoint);
  for (let i = 0; i < BREAKPOINT_MAPPING.length; i++) {
    const [breakpoint, breakpointWidth] = BREAKPOINT_MAPPING[i];
    if (breakpointFilter && breakpointFilter.indexOf(breakpoint) === -1) {
      continue;
    }
    let stickyBreakpointWidth = breakpointWidth;
    if (previousBreakpointIndex !== -1) {
      stickyBreakpointWidth += previousBreakpointIndex <= i ? -breakpointSwitchOffset : breakpointSwitchOffset;
    }
    if (width > stickyBreakpointWidth) {
      return breakpoint;
    }
  }
  return "default";
}

// node_modules/@cloudscape-design/components/internal/hooks/use-mobile/index.js
var forceMobileModeSymbol = Symbol.for("awsui-force-mobile-mode");
function getIsMobile() {
  var _a;
  const forceMobileMode = globalThis[forceMobileModeSymbol];
  if (typeof forceMobileMode !== "undefined") {
    return forceMobileMode;
  }
  if (typeof window === "undefined") {
    return false;
  }
  if (window.matchMedia) {
    return (_a = window.matchMedia(`(max-width: ${mobileBreakpoint}px)`)) === null || _a === void 0 ? void 0 : _a.matches;
  }
  return getMatchingBreakpoint(window.innerWidth, ["xs"]) !== "xs";
}
var useMobile = createSingletonState({
  initialState: () => getIsMobile(),
  factory: (handler) => {
    const listener = () => handler(getIsMobile());
    window.addEventListener("resize", listener);
    return () => {
      window.removeEventListener("resize", listener);
    };
  }
});

// node_modules/@cloudscape-design/components/container/use-sticky-header.js
var import_react = __toESM(require_react());

// node_modules/@cloudscape-design/components/internal/generated/styles/tokens.js
var spaceScaledS = "var(--space-scaled-s-8ozaad, 12px)";

// node_modules/@cloudscape-design/components/internal/styles/global-vars.js
var globalVars = {
  stickyVerticalTopOffset: "--awsui-sticky-vertical-top-offset",
  stickyVerticalBottomOffset: "--awsui-sticky-vertical-bottom-offset"
};
var global_vars_default = globalVars;

// node_modules/@cloudscape-design/components/container/use-sticky-header.js
function computeOffset({ isMobile, __stickyOffset, __mobileStickyOffset, hasInnerOverflowParents, __additionalOffset }) {
  const localOffset = isMobile ? (__stickyOffset !== null && __stickyOffset !== void 0 ? __stickyOffset : 0) - (__mobileStickyOffset !== null && __mobileStickyOffset !== void 0 ? __mobileStickyOffset : 0) : __stickyOffset !== null && __stickyOffset !== void 0 ? __stickyOffset : 0;
  if (hasInnerOverflowParents || __stickyOffset !== void 0) {
    return `${localOffset}px`;
  }
  const globalOffset = `var(${global_vars_default.stickyVerticalTopOffset}, 0px)`;
  return `calc(${globalOffset} + ${localOffset}px + ${__additionalOffset ? spaceScaledS : "0px"})`;
}
var StickyHeaderContext = (0, import_react.createContext)({
  isStuck: false,
  isStuckAtBottom: false
});
var useStickyHeader = (rootRef, headerRef, __stickyHeader, __stickyOffset, __mobileStickyOffset, __disableMobile, __additionalOffset = false) => {
  const isMobile = useMobile();
  const disableSticky = isMobile && __disableMobile;
  const isSticky = !disableSticky && !!__stickyHeader;
  const [hasInnerOverflowParents, setHasInnerOverflowParents] = (0, import_react.useState)(false);
  const [isStuck, setIsStuck] = (0, import_react.useState)(false);
  const [isStuckAtBottom, setIsStuckAtBottom] = (0, import_react.useState)(false);
  (0, import_react.useLayoutEffect)(() => {
    if (rootRef.current) {
      const overflowParents = getOverflowParents(rootRef.current);
      const mainElement = findUpUntil(rootRef.current, (elem) => elem.tagName === "MAIN");
      setHasInnerOverflowParents(overflowParents.length > 0 && overflowParents[0] !== mainElement);
    }
  }, [rootRef]);
  const computedOffset = computeOffset({
    isMobile,
    __stickyOffset,
    __mobileStickyOffset,
    hasInnerOverflowParents,
    __additionalOffset
  });
  const stickyStyles = isSticky ? {
    style: {
      top: computedOffset
    }
  } : {};
  const checkIfStuck = (0, import_react.useCallback)(({ isTrusted, target, type }) => {
    if (type === "resize" && target === window && !isTrusted) {
      return;
    }
    if (rootRef.current && headerRef.current) {
      const rootTopBorderWidth = parseFloat(getComputedStyle(rootRef.current).borderTopWidth) || 0;
      const rootTop = Math.round(rootRef.current.getBoundingClientRect().top + rootTopBorderWidth);
      const headerTop = Math.round(headerRef.current.getBoundingClientRect().top);
      if (rootTop < headerTop) {
        setIsStuck(true);
      } else {
        setIsStuck(false);
      }
      const rootBottom = Math.round(rootRef.current.getBoundingClientRect().bottom - rootTopBorderWidth);
      const headerBottom = Math.round(headerRef.current.getBoundingClientRect().bottom);
      if (rootBottom <= headerBottom) {
        setIsStuckAtBottom(true);
      } else {
        setIsStuckAtBottom(false);
      }
    }
  }, [rootRef, headerRef]);
  (0, import_react.useEffect)(() => {
    if (isSticky) {
      const controller = new AbortController();
      window.addEventListener("scroll", checkIfStuck, { capture: true, signal: controller.signal });
      window.addEventListener("resize", checkIfStuck, { signal: controller.signal });
      return () => {
        controller.abort();
      };
    }
  }, [isSticky, checkIfStuck]);
  return {
    isSticky,
    isStuck,
    isStuckAtBottom,
    stickyStyles
  };
};

// node_modules/@cloudscape-design/components/internal/context/collection-label-context.js
var import_react2 = __toESM(require_react());
var CollectionLabelContext = (0, import_react2.createContext)({});

// node_modules/@cloudscape-design/components/internal/context/container-header.js
var import_react3 = __toESM(require_react());
var ContainerHeaderContext = (0, import_react3.createContext)({ isInContainer: false });
var ContainerHeaderContextProvider = ({ children }) => {
  return import_react3.default.createElement(ContainerHeaderContext.Provider, { value: { isInContainer: true } }, children);
};
var useContainerHeader = () => {
  const { isInContainer } = (0, import_react3.useContext)(ContainerHeaderContext);
  return isInContainer;
};

// node_modules/@cloudscape-design/components/internal/context/info-link-label-context.js
var import_react4 = __toESM(require_react());
var InfoLinkLabelContext = (0, import_react4.createContext)(void 0);

// node_modules/@cloudscape-design/components/internal/context/table-component-context.js
var import_react5 = __toESM(require_react());
var TableComponentsContext = (0, import_react5.createContext)(null);
var TableComponentsContextProvider = TableComponentsContext.Provider;
var useTableComponentsContext = () => {
  const tableComponentContext = (0, import_react5.useContext)(TableComponentsContext);
  return tableComponentContext;
};

// node_modules/@cloudscape-design/components/header/styles.css.js
import "/Users/divyadharshini/Documents/Python/StockVision-AI/frontend/node_modules/@cloudscape-design/components/header/styles.scoped.css";
var styles_css_default = {
  "root": "awsui_root_2qdw9_1pryo_185",
  "refresh": "awsui_refresh_2qdw9_1pryo_221",
  "root-no-actions": "awsui_root-no-actions_2qdw9_1pryo_221",
  "root-variant-h2": "awsui_root-variant-h2_2qdw9_1pryo_224",
  "root-variant-h3": "awsui_root-variant-h3_2qdw9_1pryo_224",
  "root-has-description": "awsui_root-has-description_2qdw9_1pryo_224",
  "root-variant-h1": "awsui_root-variant-h1_2qdw9_1pryo_233",
  "main": "awsui_main_2qdw9_1pryo_243",
  "no-wrap": "awsui_no-wrap_2qdw9_1pryo_253",
  "main-variant-h1": "awsui_main-variant-h1_2qdw9_1pryo_259",
  "actions": "awsui_actions_2qdw9_1pryo_267",
  "actions-centered": "awsui_actions-centered_2qdw9_1pryo_272",
  "actions-variant-h1": "awsui_actions-variant-h1_2qdw9_1pryo_275",
  "actions-variant-h2": "awsui_actions-variant-h2_2qdw9_1pryo_279",
  "actions-variant-h3": "awsui_actions-variant-h3_2qdw9_1pryo_283",
  "title": "awsui_title_2qdw9_1pryo_299",
  "title-variant-h1": "awsui_title-variant-h1_2qdw9_1pryo_304",
  "title-variant-h2": "awsui_title-variant-h2_2qdw9_1pryo_311",
  "title-variant-h3": "awsui_title-variant-h3_2qdw9_1pryo_317",
  "virtual-space": "awsui_virtual-space_2qdw9_1pryo_338",
  "info": "awsui_info_2qdw9_1pryo_344",
  "description": "awsui_description_2qdw9_1pryo_348",
  "description-variant-h1": "awsui_description-variant-h1_2qdw9_1pryo_355",
  "description-variant-h2": "awsui_description-variant-h2_2qdw9_1pryo_359",
  "description-variant-h3": "awsui_description-variant-h3_2qdw9_1pryo_366",
  "heading": "awsui_heading_2qdw9_1pryo_377",
  "heading-variant-h1": "awsui_heading-variant-h1_2qdw9_1pryo_402",
  "heading-variant-h2": "awsui_heading-variant-h2_2qdw9_1pryo_407",
  "heading-variant-h3": "awsui_heading-variant-h3_2qdw9_1pryo_412",
  "heading-text": "awsui_heading-text_2qdw9_1pryo_418",
  "heading-text-variant-h1": "awsui_heading-text-variant-h1_2qdw9_1pryo_421",
  "heading-text-variant-h2": "awsui_heading-text-variant-h2_2qdw9_1pryo_430",
  "heading-text-variant-h3": "awsui_heading-text-variant-h3_2qdw9_1pryo_439",
  "counter": "awsui_counter_2qdw9_1pryo_449"
};

export {
  browserScrollbarSize,
  matchBreakpointMapping,
  getMatchingBreakpoint,
  useMobile,
  global_vars_default,
  StickyHeaderContext,
  useStickyHeader,
  CollectionLabelContext,
  ContainerHeaderContextProvider,
  useContainerHeader,
  InfoLinkLabelContext,
  TableComponentsContextProvider,
  useTableComponentsContext,
  styles_css_default
};
//# sourceMappingURL=chunk-FU4LRFOW.js.map
